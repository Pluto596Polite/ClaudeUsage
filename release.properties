# Version to publish. Changing this file on the development branch triggers the Release
# workflow, which builds a signed APK and publishes a GitHub Release (and its tag).
# For normal use, prefer pushing a git tag directly: `git tag v1.2.0 && git push origin v1.2.0`.
version=v1.1.0
