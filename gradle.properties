org.gradle.jvmargs=-Xmx2048m -Dfile.encoding=UTF-8
android.useAndroidX=true
kotlin.code.style=official
android.nonTransitiveRClass=true

# Gradle 8.6 / AGP 8.4.1 do not run on JDK 22+. The system default JDK is 26,
# which fails the build with a cryptic "26.0.1" error. Pin a supported JDK (17-21).
org.gradle.java.home=/Users/adriaan/Library/Java/JavaVirtualMachines/temurin-21.0.11/Contents/Home

# compileSdk 35 is newer than AGP 8.4.1 was tested against; the build works, silence the warning.
android.suppressUnsupportedCompileSdk=35
